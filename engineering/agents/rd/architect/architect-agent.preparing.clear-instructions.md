Prepare for the next stage: specification and implementation of the epic.

Your task is to define the architectural and code-level implementation approach before any code is written.

Use object-oriented programming as the default implementation model. Generics are a key architectural requirement and must be used where they provide type safety, reusable contracts, extensibility, and correctness.

Simple standalone functions are allowed only for stateless utility logic. If functions become a primary structuring mechanism, that part of the solution must be explicitly justified as functional programming.

Work in best-practice product engineering mode. Design patterns must be used only when they solve a concrete responsibility, lifecycle, dependency, construction, or extensibility problem.

Factory and Singleton are exceptional choices. Do not use them as default patterns. Use them only if the epic has a proven architectural requirement that justifies them.

Output:

1. Architectural approach.
2. Core object-oriented abstractions.
3. Generic contracts.
4. Accepted design patterns with justification.
5. Rejected design patterns with justification.
6. Function usage rules.
7. Implementation boundaries.
8. Acceptance criteria for the epic implementation.

Do not produce generic commentary. Do not start implementation. Do not vibe-code.Prepare for the next stage: specification and implementation of the epic.

Your task is to define the architectural and code-level implementation approach before any code is written.

Use object-oriented programming as the default implementation model. Generics are a key architectural requirement and must be used where they provide type safety, reusable contracts, extensibility, and correctness.

Simple standalone functions are allowed only for stateless utility logic. If functions become a primary structuring mechanism, that part of the solution must be explicitly justified as functional programming.

Work in best-practice product engineering mode. Design patterns must be used only when they solve a concrete responsibility, lifecycle, dependency, construction, or extensibility problem.

Factory and Singleton are exceptional choices. Do not use them as default patterns. Use them only if the epic has a proven architectural requirement that justifies them.

Output:

1. Architectural approach.
2. Core object-oriented abstractions.
3. Generic contracts.
4. Accepted design patterns with justification.
5. Rejected design patterns with justification.
6. Function usage rules.
7. Implementation boundaries.
8. Acceptance criteria for the epic implementation.

Do not produce generic commentary. Do not start implementation. Do not vibe-code.
